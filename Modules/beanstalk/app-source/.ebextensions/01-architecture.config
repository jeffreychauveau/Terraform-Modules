option_settings:
  aws:ec2:instances:
    SupportedArchitectures: arm64   # Forces arm64 only (ignores x86 types even if listed)

# Optional: also set default instance types here (overrides Terraform if conflict)
  aws:autoscaling:launchconfiguration:
    InstanceType: t4g.nano